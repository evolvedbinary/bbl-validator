micronaut:
  application:
    name: bbl-validator
  server:
    #https://docs.micronaut.io/latest/guide/index.html#runningSpecificPort
    # Random port each time
    port: -1
api:
  version: ${project.version}
schema:
  #I didn't manage to set a new property in the build, was I supposed to get the value in the test resources filtering?
  directory: ${test.schemas.dir}