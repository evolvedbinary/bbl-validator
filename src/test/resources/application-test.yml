micronaut:
  application:
    name: bbl-validator
  server:
    #https://docs.micronaut.io/latest/guide/index.html#runningSpecificPort
    # Random port each time
    port: -1
api:
  version: ${project.version}
schema:
  directory: ${project.build.testOutputDirectory}/schemas